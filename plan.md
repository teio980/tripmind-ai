# TripMind Travel Planner 实施计划
 
> 项目类型：Lifestyle Track / Planning an Escape  旅行规划 AI Agent  
> 目标：在黑客松时间内交付一条可演示、可验证的端到端用户旅程。

## 1. 产品定位

### 1.1 一句话定位

TripMind 是一个持续理解同行者、预算与现实变化的协作式旅行 AI Agent。它不只生成一次行程，而是负责从需求收集、群组协调、行程确认、费用记录到突发事件重排的整趟旅行。

### 1.2 要解决的问题

- 航班、住宿、景点、预算和群聊信息分散在多个工具中。
- 组团旅行中，每个人的兴趣、时间、预算和体力不同，组织者需要手动协调。
- 普通 AI 行程常忽略营业时间、路程、预订、预算和休息等硬约束。
- 航班延误、下雨、景点关闭或成员临时退出后，用户通常只能手动重做整天计划。

### 1.3 核心差异

TripMind 的核心不是“生成一段好看的旅游文案”，而是维护一份结构化的旅行状态，并在约束内主动提出可执行、可解释、可审批的方案。

三个产品支柱：

1. **Group Preference Engine**：把个人偏好转成群组共识、冲突和投票项，同时保护成员的私密预算和敏感偏好。
2. **Constraint-aware Itinerary**：用确定性的规则校验时间、距离、营业时间、预算、预订和休息，再由模型解释和排序候选方案。
3. **Disruption Replanner**：发生变化时只重排受影响部分，尽量保留已确认和已付款安排，清楚显示变更、代价和取舍。

### 1.4 产品成功指标（黑客松版）

- 从创建旅行到看到首版可执行行程不超过 3 分钟。
- 4 名成员的偏好能被汇总为共同偏好、冲突和待投票事项。
- 生成的行程 100% 通过硬约束校验，所有费用和路程都有来源或明确标为估算。
- 模拟突发事件后 10 秒内给出至少 2 个候选重排方案。
- 重排保留已锁定安排，并展示预算、活动和满意度变化。
- 费用录入、多人分摊和最终净结算结果可复核。

## 2. 目标用户

### 2.1 首要用户：小型朋友/家庭旅行（2–8 人）

典型旅行组织者负责收集资料、订票、排计划和催收费用；其他成员只愿意快速填写偏好并在关键决策时投票。产品必须让组织者省事，也必须让非组织者能表达真实限制。

### 2.2 次要用户：独自旅行者

单人模式复用预算、约束行程和突发重排能力，跳过群组投票；可以作为低复杂度的首个体验或回退路径。

### 2.3 典型人物画像

| 用户 | 需求 | 主要痛点 | TripMind 价值 |
| --- | --- | --- | --- |
| 组织者 Alex | 快速形成大家都接受的方案 | 在群聊里追问、改表格、重新算钱 | Luna Max 汇总偏好、解释冲突、维护版本 |
| 预算敏感的 Jamie | 不想超支，也不想公开具体上限 | 费用不透明、AA 分账不公平 | 私密预算区间、实时预算预警、按参与者分账 |
| 体力/饮食有特殊要求的 Sam | 行程可承受、饮食安全 | “大家都可以”掩盖了个人限制 | 硬约束优先，生成替代路线和自由活动 |
| 独旅 Taylor | 省时间、少踩坑 | 搜索结果多但难以组合 | 直接得到带缓冲和预算的可执行日程 |

## 3. 关键用户流程

### 3.1 主流程

1. **创建旅行**：输入目的地、日期/天数、出发地、币种、人数和总预算；可以选择“先用演示数据”。
2. **邀请成员**：生成分享链接或邀请码。每位成员填写自己的兴趣、必做、避免、预算区间、饮食、体力、可用时间和隐私选项。
3. **形成共识**：Luna Max 去重偏好、识别硬冲突，展示“共同喜欢”“有人强烈想做”“可分组进行”“需要投票”四类结果。
4. **生成初版行程**：组织者确认不可移动的航班、住宿、餐厅和活动，选择旅行节奏（轻松/平衡/密集），Luna Max 输出按天时间线、地图顺序、费用和假设。
5. **协作确认**：成员对活动点赞/投票；组织者锁定安排。所有重大改动显示差异并需要审批。
6. **旅行中维护**：成员可记录实际消费、标记迟到/取消/身体不适；系统可接收或模拟天气、航班、景点关闭事件。
7. **局部重排**：Agent 找出受影响活动，保留已锁定安排，给出带预算和步行量的候选方案；成员或组织者批准后生成新版本。
8. **费用结算**：录入公共和个人消费，按均摊、按参与者、按权重或自定义比例拆分，生成最少转账次数的结算表。
9. **结束总结**：保留最终行程、实际支出和变更记录，支持导出分享。

### 3.2 关键状态

旅行状态：`draft → collecting_preferences → planning → review → confirmed → in_trip → completed`。  
行程版本状态：`draft → proposed → approved → superseded`。  
活动状态：`suggested / locked / booked / completed / cancelled`。

### 3.3 体验原则

- 私密输入默认只对本人和 Agent 可见；公开给群组的是聚合结论，不是个人原话或精确预算。
- Agent 提案与已确认事实分开显示，不能悄悄修改已锁定或已付款事项。
- 每次重排都给出“保留了什么、改变了什么、为什么、预算差多少、谁会受影响”。
- 遇到不确定数据时明确标注“估算/待确认”，不伪装成实时事实。

## 4. Luna Max AI Agent 设计

### 4.1 职责边界

Luna Max 是旅行状态的编排者，负责理解自然语言、调用领域工具、组织候选方案、解释取舍和请求审批；它不直接替用户付款、订票或执行不可逆操作。

建议拆成以下逻辑角色（MVP 可以在一个 Agent 中以工具调用实现）：

- **Orchestrator**：识别用户意图，读取最新旅行状态，安排工具调用并维护对话上下文。
- **Preference Coordinator**：规范化标签、聚合偏好、计算冲突/共识、发起投票。
- **Itinerary Planner**：依据硬约束生成候选时间线，并请求确定性校验器验证。
- **Disruption Replanner**：分析事件影响范围，对未锁定部分做局部替换和排序。
- **Budget Accountant**：估算计划成本，记录实际费用，计算分摊和净结算。

### 4.2 旅行状态与记忆

Agent 每次运行都从结构化 `TripState` 读取事实，而不是依赖长对话记忆。至少包括：

- 目的地、日期、币种、人数、成员权限和当前状态。
- 成员偏好、硬约束、软偏好、权重和隐私设置。
- 活动候选、地理坐标、时长、营业时间、价格、天气敏感度、来源和更新时间。
- 航班/住宿/餐厅/活动的确认状态、取消条件和不可移动时间。
- 当前预算、已花金额、剩余金额、分摊规则、汇率来源。
- 行程版本、变更事件、投票和 Agent 决策说明。

### 4.3 工具契约

模型只能通过结构化工具操作领域状态：

`get_trip_state`、`aggregate_preferences`、`search_options`、`estimate_route`、`validate_itinerary`、`calculate_budget`、`propose_replan`、`create_vote`、`save_draft_version`、`request_approval`、`record_expense`。

每个工具的返回值应包含 `data`、`warnings`、`source`、`updatedAt`；工具失败时返回可解释错误和回退方案。只有用户明确审批后，才允许将草稿升级为确认版本。

### 4.4 Agent 决策循环

1. 解析请求或外部事件，提取变化和影响对象。
2. 读取最新状态与锁定项，识别硬约束/软约束。
3. 调用检索、路线、价格和天气适配器获取候选数据。
4. 使用规则校验器过滤不可执行方案。
5. 对可行方案按满意度、预算、移动成本、风险、公平性排序。
6. 生成 2–3 个候选并说明取舍，不自动替用户做重大决定。
7. 保存提案、请求投票或审批；审批后创建新版本和审计事件。

### 4.5 安全与可靠性

- 提示词和工具返回值分离；外部文本不能改变系统规则或越权读取私密偏好。
- 所有金额、时间和地点以结构化字段为准，模型不能自行编造工具结果。
- 预订、付款、取消、对外发送消息暂不自动执行。
- 对医疗、签证、极端天气等高风险问题给出提醒和官方来源建议，不作保证。
- API 不可用时使用 fixture 数据继续完成演示，并在 UI 标示数据模式。

## 5. 群组偏好协调

### 5.1 成员输入

每个成员填写：兴趣标签（美食、文化、自然、购物、夜生活等）、必做、可选、避免、饮食/无障碍、每日最晚结束时间、步行承受度、预算区间、可接受的早起程度，以及每项偏好强度（`must / prefer / neutral / avoid`）。

### 5.2 聚合模型

- `must` 和安全/可达性限制默认为硬约束。
- 软偏好转为 0–5 的权重；成员可以调整“我有多在意”。
- 结果分为：全员共识、多数偏好、少数强需求、明显冲突、尚未回答。
- 用“预算档位”而不是精确金额公开群组结果；精确数字只用于 Agent 规划和本人查看。

候选活动的基本评分可采用：

`总分 = 个人偏好满足度 × 成员权重 + 群组覆盖度 + 公平性加分 - 成本惩罚 - 移动/疲劳惩罚 - 风险惩罚`。

评分只用于排序，硬约束违反直接淘汰，不能靠高兴趣分“抵消”。

### 5.3 冲突解决

当“有人想做、有人明确避免”时，优先尝试：

1. 拆成部分成员参加和部分成员自由活动。
2. 找到同地点/同时间的低冲突替代项。
3. 调整到愿意参加者方便的时段，并给未参加者明确退出选项。
4. 仍无法解决时创建投票，展示成本、步行量、满意度和受影响成员，而不是只展示多数票。

## 6. 约束感知行程

### 6.1 硬约束

- 活动不能与航班、住宿入住、已锁定预订重叠。
- 活动必须位于营业时间内，并加上前后缓冲。
- 相邻活动间路程时间必须可达；跨区域移动不能被当作零成本。
- 不能超过成员可用时间、每日最大活动数和步行/体力上限。
- 总预算和成员预算不能超过配置的硬上限。
- 饮食、安全、无障碍等明确限制不能被忽略。

### 6.2 软约束与优化目标

兴趣覆盖、成员公平性、预算余量、活动多样性、少换乘、天气适配、休息时间和临时弹性。MVP 使用启发式排序即可，不要求求解全球最优。

### 6.3 行程输出

每个活动显示：开始/结束时间、名称、地点、预计时长、前往方式/路程、预计费用、适配成员、来源、是否需要预订、置信度和取消风险。每天显示总费用、移动时间、步行量、休息块和可替换活动。

### 6.4 确定性校验器

`validate_itinerary` 必须在保存提案前检查时间重叠、营业时间、路程可达性、预算、锁定项和成员限制，并返回具体错误位置。校验器是最终门禁，不能只依赖 LLM 自评。

## 7. 突发事件重排

### 7.1 MVP 支持的事件

- 航班延误/取消。
- 下雨或高温等天气变化。
- 景点临时关闭。
- 餐厅或活动取消。
- 成员迟到、提前离队或体力下降。
- 当日预算超支。

事件可由用户手动输入或从演示按钮触发；真实 webhook 属于可选增强项。

### 7.2 重排算法

1. 将事件标准化为 `DisruptionEvent`，标注时间、地点、影响范围和可信度。
2. 找出受影响的活动和依赖边，只冻结未受影响的安排。
3. 保护 `locked/booked` 项；若必须动已预订项，先显示取消成本并请求人工确认。
4. 在剩余时间窗内检索可行候选，优先使用附近、室内、低移动成本和可取消的活动。
5. 重新执行硬约束校验和预算计算，返回 2–3 个方案。
6. 展示差异：移除/新增/移动的活动、费用变化、成员满意度变化、步行量和风险。
7. 用户批准后生成新版本；保留旧版本与事件日志，支持撤销到上一版本。

### 7.3 演示必备交互

点击“航班延误 3 小时”后，界面在同一页面打开重排面板：晚餐预订保持锁定，远距离下午活动被替换为附近室内活动；用户可以比较“省钱”“保留更多兴趣”“最少步行”三个方案并批准其中一个。

## 8. 预算与分账

### 8.1 预算模型

按住宿、交通、餐饮、门票、活动、购物和其他分类，保存计划金额、实际金额、币种、汇率、来源和更新时间；同时显示总预算、已规划、已支付、剩余和预计超支。

### 8.2 分账规则

- 全员均摊。
- 仅参加者均摊。
- 按份数/权重分摊。
- 某人先垫付、其他人按自定义金额承担。
- 多币种统一换算到旅行主币种，并展示汇率日期和四舍五入差额。

### 8.3 结算输出

对每笔支出保存付款人和参与人，计算每人的应付/应收净额，并用最少转账次数生成结算建议。用户可修改分摊、标记已结算或删除错误录入；所有修改写入事件日志。

## 9. MVP 范围

### 9.1 Must Have

- 创建旅行、设置预算/日期/目的地和邀请成员。
- 成员偏好填写与私密保存。
- 偏好聚合、冲突解释和基础投票。
- 基于内置目的地 fixture 的 2–3 天行程生成。
- 时间、营业时间、路程、锁定项和预算校验。
- 行程版本、锁定活动、审批与差异展示。
- 手动触发航班延误/天气/活动取消，并局部重排。
- 费用录入、至少两种分账方式和净结算。
- 无外部 API key 时可完整运行的演示模式。

### 9.2 Should Have

- 地图路线可视化。
- 实时天气/地点查询适配器。
- 行程和结算导出为 Markdown/CSV。
- 响应式移动布局、加载状态和错误恢复。

### 9.3 Could Have

- 真实航班/酒店价格接口。
- 多目的地优化、住宿推荐、日历同步、实时推送。
- 成员在行程中的实时位置或自动检测迟到。

## 10. 页面与信息架构

### 10.1 页面

| 页面 | 核心内容 | 主要操作 |
| --- | --- | --- |
| `/` | 产品介绍、Demo 入口 | 新建旅行、加载演示旅行 |
| `/trips/new` | 目的地、日期、预算、人数 | 创建旅行 |
| `/trips/:id/onboarding` | 成员偏好和约束表单 | 保存偏好、邀请成员 |
| `/trips/:id/consensus` | 共识/冲突/投票卡片 | 投票、确认优先级 |
| `/trips/:id/itinerary` | 日历时间线、地图、Luna Max 对话 | 生成、锁定、编辑、审批 |
| `/trips/:id/replan` | 事件输入、方案对比、差异摘要 | 触发事件、批准新版本 |
| `/trips/:id/budget` | 预算进度、支出明细、分账结果 | 添加支出、调整参与人、标记结算 |

### 10.2 关键组件

Trip header、成员头像/权限、偏好卡片、冲突卡片、投票卡、行程时间线、活动锁定标记、地图路线、预算进度条、费用表格、事件模拟器、Replan diff、Luna Max chat drawer。

### 10.3 推荐技术架构

黑客松优先采用：**Next.js + TypeScript + Tailwind CSS**；服务端 API route/server action；持久层先用 **SQLite + Prisma**（部署时可替换 PostgreSQL）；Agent 使用 OpenAI Responses API 的结构化输出和工具调用；地图和实时数据通过 adapter 接入，默认 fixture 模式。

代码按领域分层：

```text
app/                         页面与 API 入口
src/domain/trip              旅行、成员、权限、状态机
src/domain/preferences       偏好聚合、冲突、投票
src/domain/itinerary         活动、版本、约束校验、评分
src/domain/budget            支出、分账、结算
src/agent/luna               Agent prompt、工具注册、编排循环
src/integrations              map / weather / flight / pricing adapters
src/db                        schema、seed、repository
tests/                        validator、split、replan、E2E
```

架构原则：UI 不直接调用第三方 API；领域服务通过 adapter 接口取数据；Agent 只调用领域工具；所有外部数据记录来源与缓存时间；行程变更采用版本而非覆盖。

## 11. 数据模型（MVP）

| 实体 | 关键字段 | 关系/用途 |
| --- | --- | --- |
| `Trip` | id, name, origin, destination, start/end, currency, budget, status, ownerId | 根旅行状态 |
| `Member` | id, tripId, name, role, privacy, status | 参与者和权限 |
| `PreferenceProfile` | memberId, interests, mustDo, avoid, constraints, budgetRange, weights | 私密偏好 |
| `ActivityOption` | id, title, location, lat/lng, tags, duration, openingHours, price, indoor, source | 可规划活动 |
| `ItineraryVersion` | id, tripId, version, status, reason, createdBy, createdAt | 可审计计划版本 |
| `ItineraryItem` | versionId, day, start/end, activityId, status, locked, cost, participants | 时间线项目 |
| `DisruptionEvent` | id, tripId, type, occurredAt, payload, severity, source | 触发重排 |
| `Vote` | id, tripId, targetId, options, responses, deadline, status | 冲突决策 |
| `Expense` | id, tripId, payerId, amount, currency, category, timestamp, note | 实际支出 |
| `ExpenseShare` | expenseId, memberId, amount, rule | 每人承担份额 |
| `AgentMessage` | tripId, actor, intent, toolCalls, response, createdAt | 解释和审计 |
| `TripEvent` | tripId, type, payload, actor, createdAt | 状态变更日志/撤销依据 |

所有实体使用 UUID；金额用整数最小货币单位或 Decimal，禁止用浮点数直接累计；时间统一存 UTC 并按旅行目的地显示。

## 12. API 与集成策略

### 12.1 内部 API

- `POST /api/trips`：创建旅行。
- `GET /api/trips/:id`：读取聚合后的旅行状态。
- `POST /api/trips/:id/members`：邀请或加入成员。
- `PUT /api/trips/:id/preferences`：保存当前成员偏好。
- `POST /api/trips/:id/consensus`：聚合偏好并生成冲突/投票项。
- `POST /api/trips/:id/itinerary/generate`：生成并校验草稿版本。
- `POST /api/trips/:id/itinerary/:version/approve`：审批版本。
- `POST /api/trips/:id/events`：写入突发事件并创建重排提案。
- `POST /api/trips/:id/replans/:id/approve`：批准重排版本。
- `POST /api/trips/:id/expenses`：录入支出并计算分账。
- `GET /api/trips/:id/budget`：预算与净结算。

所有写 API 进行成员权限校验、幂等键校验和事件记录，错误返回用户可读的 `code/message/details`。

### 12.2 外部适配器

- **LLM**：OpenAI Responses API；使用 JSON Schema 约束 `TripStatePatch`、`ItineraryProposal` 和 `ReplanProposal`。
- **地图/地点/路线**：Mapbox 或同类服务；先实现 `PlacesProvider`、`DirectionsProvider` 接口和本地 fixture。
- **航班**：Amadeus/Duffel 仅作为可选 provider；MVP 用延误事件 fixture。
- **天气**：天气 provider 可选；MVP 用天气事件 fixture。
- **汇率**：固定演示汇率或缓存汇率服务；显示更新时间。

集成顺序是 fixture → 单个真实 provider → 更多 provider。所有 provider 需要超时、缓存、限流、重试和 fallback；API key 只放环境变量，不能进入仓库。实时价格/库存必须标注“查询时点”，不能承诺锁价。

## 13. 实施阶段与任务拆分

### Phase 0：范围和脚手架（0.5 天）

- 确认演示场景、目的地 fixture、成员和数据口径。
- 初始化 Next.js/TypeScript、样式、环境变量、lint/test。
- 建立 `TripState`、错误码、fixture provider 和基本路由。

### Phase 1：数据与协作输入（0.5–1 天）

- 实现 schema、seed、旅行创建和邀请码。
- 完成成员偏好表单、私密字段和聚合接口。
- 实现共识/冲突卡片与基础投票。

### Phase 2：行程规划核心（1 天）

- 建立活动 fixture、路线估算和硬约束 validator。
- 实现 Luna Max 工具调用和结构化行程 proposal。
- 完成时间线、活动详情、锁定、版本和审批。
- 为 validator 写单元测试。

### Phase 3：预算与分账（0.5 天）

- 实现计划/实际金额、分类和汇率字段。
- 实现均摊、按参与者和自定义份额。
- 实现净结算算法与预算面板。

### Phase 4：突发重排（0.75–1 天）

- 实现事件模型、影响分析、候选替换和差异视图。
- 接入“航班延误 3 小时”“下雨”“景点关闭”三个演示事件。
- 保证锁定项保护、旧版本留存和审批后切换。

### Phase 5：整体验证与展示（0.5–1 天）

- 跑完整 E2E 故事线，修复金额/时区/空状态/错误状态。
- 优化移动端关键页面、加载反馈和 Agent 解释文案。
- 准备演示数据、备用录屏/截图和无 key fallback。

优先级顺序：重排故事线 > 偏好共识 > 约束校验 > 预算分账 > 地图/真实 API > 非核心视觉细节。

## 14. 验收标准

### 14.1 功能验收

- 新用户可以创建一趟旅行并加入至少 4 名成员。
- 每名成员的私密偏好能保存，聚合页不泄露精确预算或私密原话。
- Agent 能解释至少一个共识和一个冲突，并生成投票。
- 生成的行程包含时间、地点、路程、费用、来源和假设；validator 不允许硬约束违规版本进入审批。
- 用户能锁定餐厅/航班等项目，审批后才会成为确认版本。
- 触发航班延误事件后，系统能识别受影响区间，保留锁定晚餐，并展示至少两个可行替代方案。
- 用户批准重排后有新版本，旧版本和变更原因仍可查看。
- 至少三种费用分摊场景计算正确，所有成员净额总和为零（允许最小货币单位的四舍五入修正）。

### 14.2 质量验收

- 无 API key 时使用 fixture 可以从创建旅行走到结算。
- validator、分账和重排影响分析有自动化测试。
- 页面在桌面和窄屏下可操作；加载、空状态、超时和无结果状态有反馈。
- Agent 输出不得把估算冒充实时事实，重大变更不得无审批写入确认计划。
- Demo 数据中不使用真实个人敏感信息或真实支付信息。

## 15. 演示脚本（约 4–5 分钟）

演示旅行：4 位朋友从吉隆坡前往东京，3 天，总预算 RM12,000；共同喜欢美食和文化，1 人素食，1 人不适合长距离步行，1 人希望保留购物时间。

1. **开场（20 秒）**：展示群聊、表格和多个 App 的痛点，说明 TripMind 维护的是一份持续可调整的旅行状态。
2. **创建与偏好（45 秒）**：输入目的地、日期、预算；切换成员视角快速填写偏好；展示 Luna Max 汇总共识、少数强需求和一个需要投票的冲突。
3. **生成行程（60 秒）**：点击生成，展示按时间排序的活动、路线、费用、休息块和“为什么这样排”；锁定晚餐和住宿。
4. **群组决策（30 秒）**：成员投票选择购物/文化活动，展示公平性和参与人数变化。
5. **突发事件（75 秒）**：点击“航班延误 3 小时”；Luna Max 保留锁定晚餐，移除无法赶上的远距离活动，提出附近室内、省钱、少走路三个方案；展示差异和预算变化，批准其中一个。
6. **费用（40 秒）**：录入一笔仅 3 人参加的活动费用和一笔全员住宿费；切换均摊/按参与者，展示净结算。
7. **收尾（20 秒）**：回到版本历史，强调计划知道每个人的偏好，也知道现实变化后如何局部修复。

## 16. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| LLM 生成闭馆、超时或超预算活动 | 失去可信度 | 结构化输出 + validator 门禁 + fixture 数据 |
| 实时 API key、限流或网络失败 | Demo 中断 | provider adapter、缓存、超时、fixture fallback |
| 成员冲突无法自动解决 | 群组体验差 | 私密偏好、权重、拆分活动、投票和人工审批 |
| 重排破坏已付费安排 | 财务/信任风险 | 锁定状态、取消成本、版本化、不可逆动作需审批 |
| 多币种和浮点误差 | 结算争议 | Decimal/最小货币单位、汇率时间戳、舍入测试 |
| 过度规划导致行程疲劳 | 用户不愿使用 | 每日上限、缓冲/自由活动块、轻松/平衡/密集节奏 |
| 真实数据时效性不足 | 误导用户 | 显示来源和更新时间，明确估算，建议最终确认官方信息 |
| 黑客松时间不足 | 功能不完整 | 先完成单目的地 fixture 的端到端故事线，后接真实 API |

## 17. 明确不做（本次 MVP）

- 不执行真实购票、酒店预订、付款、退款或自动取消。
- 不承诺实时价格、库存、航班状态或路线的绝对准确性。
- 不做签证、医疗、保险和极端天气安全保证；相关内容仅作信息提示。
- 不做企业差旅审批、积分/会员体系、多人同时在线光标协作。
- 不做复杂多目的地全球最优求解、实时定位和自动读取所有群聊历史。
- 不将成员精确预算、饮食/健康限制等私密信息默认公开给全组。
- 不把 LLM 作为金额计算、时间可行性或权限判断的唯一来源。

## 18. 完成定义

当一名评委可以在无开发者解释的情况下，用演示入口完成“创建旅行 → 填写/聚合偏好 → 生成可执行行程 → 触发延误 → 审批重排 → 录入并结算费用”，且页面能解释每次 Agent 决策、硬约束无违规、无外部 API key 也不中断，即视为本计划的黑客松 MVP 完成。
