# TripMind Prototype Plan

> 项目阶段：Prototype  
> 交付形态：Next.js 交互原型  
> 数据模式：槟城 Demo Fixture  
> 目标：用 8 个核心画面，在 5–6 分钟内稳定演示 TripMind 从模糊旅行想法到可解释、可审批行程版本的完整体验。

> 文档职责：`plan.md §3.5` 是共享规则、数值和版本语义的唯一来源；本文件只定义本阶段的画面范围、制作顺序、演示脚本和可观察验收；画面细节见 `prototype.md`。

## 1. Prototype 目标

本 Prototype 用于验证 TripMind 的核心产品体验，而不是提前实现完整 MVP 后端。原型必须符合 `plan.md` 第 14.7 节的提交要求：

- 提供公开可访问、无需登录的 HTTPS 链接。
- 覆盖 4–8 个关键画面；本方案采用完整 8 屏。
- 每个画面附 1–2 句 caption，说明用户动作、系统反馈和验证目标。
- 完整串联以下演示路径：

> 模糊输入 → Agent 补问 → 条件确认与生成 → 独立行程 → Harmony → Energy → 暴雨重排 → 预算与版本确认

- 使用固定 Fixture 保证没有外部 API key 或网络数据时仍可重复演示。
- 同时适配手机和桌面浏览器。

## 2. Prototype 用户与场景

### 2.1 目标用户

首要用户是只有模糊旅行想法、不知道如何开始规划的个人或小型朋友/家庭旅行组织者。

### 2.2 固定演示场景

- 普通新用户默认创建单人行程；四人、饮食和步行限制不会被系统猜测。
- 用户主动点击“加载槟城 Demo”后才载入 `Demo Fixture preset`：Alex、Jamie、Sam、Taylor 共 4 人；Sam 的素食和步行限制、Taylor 的购物偏好都标注为 `演示预设`。
- 目的地：槟城。
- 时长：3 天。
- 成员：Alex、Jamie、Sam、Taylor，共 4 人（全组/全程 Demo 口径）。
- 总预算：RM4,800；分类预算：住宿 RM1,900、餐饮 RM1,200、交通 RM700、活动 RM1,000，合计 RM4,800。
- 当前正式版本预计花费：RM4,360（住宿 1,800 + 餐饮 1,040 + 交通 540 + 活动 980），剩余 RM440；暴雨候选基于当前正式版本增加 RM40，草稿预计 RM4,400，剩余 RM400。
- 共同兴趣：美食与文化。
- 特殊需求：Sam 素食、Sam 每日步行硬上限 9.0 km、舒适目标 ≤5.0 km；Taylor 希望保留购物时间。以上都是 Demo Fixture 输入，不是系统猜测或医疗判断。
- 固定安排：Day 2 19:30 已锁定晚餐，任何优化和重排都不得静默修改。
- 暴雨：Day 2 13:30 触发，影响原定 14:00–17:00 户外活动；替代活动必须在 17:00 前结束，并通过至少 30 分钟预计交通和 60 分钟到晚餐的缓冲校验。
- Energy：上述 8.4 km → 4.6 km、休息 0 → 1 都是 Day 2 的系统估算；初始 8.4 km 未超过 9.0 km 硬上限，但可显示 `High` 风险，High 不等于硬约束违规。
- Harmony：`72% → 91%` 是待批准候选的系统估算偏好匹配度。画面展示活动调整理由；成员真实反馈只在主动提交后显示，Demo 默认标为“未收集”。

## 3. 八个核心画面

### Screen 1：首页 AI Chatbox

**路由：** `/`

核心内容：

- Chatbox 是首屏第一视觉和主要操作，不要求用户先创建 Plan。
- 显示示例提示词、最近行程和“加载槟城 Demo”入口。
- 默认演示输入：“我想去旅行，但还没有任何计划”。
- 先保留普通新用户的单人入口（人数 1 人）；演示者在不跳过模糊输入的前提下主动点击“加载槟城 Demo”，再载入四人预设和后续限制，并在画面上标注来源。
- 提交后模拟 Business Scope Guard，通过后进入 Agent 补问。
- 非旅行请求返回固定引导，不改变任何 Prototype 状态：

> 我只负责 TripMind 的旅行规划与行程管理。你可以告诉我目的地、日期，或者想怎样修改现有行程。

**Caption：** 用户无需理解产品结构或填写长表单，只需说出一个模糊想法即可开始规划。

### Screen 2：Agent 补问

**路由：** `/chat/demo`

核心内容：

- 显示完整对话、已提取条件和仍缺失的必要字段。
- Agent 只追问目的地，以及日期范围或旅行天数。
- 提供快捷回答：“槟城，3 天，四个人，喜欢美食，不想太赶”，其中“四个人”和 Demo 限制只在用户主动加载 `Demo Fixture preset` 时出现。
- 普通新用户显示人数 `1 人（默认单人）`；支持“你决定”时，只对确实采用的可选值标记 `系统默认`，不把人数、饮食或步行限制伪装成推测。
- 条件完整后进入生成前确认，不重复提问。

**Caption：** Agent 每轮只补问会阻止行程生成的问题，并保留用户已经提供的信息。

### Screen 3：条件确认与生成

**路由：** `/chat/demo?stage=ready`

核心内容：

- 以结构化卡片显示目的地、天数、人数、兴趣、节奏、限制和默认假设。
- 用户点击“生成 3 天行程”后，依次模拟以下阶段：
  1. 理解需求。
  2. 搜索活动候选。
  3. 估算路线与费用。
  4. 执行硬约束校验。
  5. 保存首版行程。
- 页面明确显示 `Demo data`、`Estimated` 和“待最终确认”标签。
- 若加载 Demo，明确显示 `Demo Fixture preset · 4 人`；普通新用户保持单人。
- 生成完成后自动进入独立行程页。

**Caption：** TripMind 将模型输出转成结构化条件，并在保存前经过约束校验，而不是直接返回一段旅游文案。

### Screen 4：独立行程页

**路由：** `/trips/penang-demo/itinerary`

核心内容：

- 显示稳定 Trip ID、3 天标签、时间线、地点、移动时间、预计费用和数据来源。
- 标记锁定晚餐、当前版本、估算信息和最后更新时间。
- 提供 Agent 抽屉、邀请成员、Harmony、Energy、重排和预算入口。
- 用户输入“第二天不要太早”后，展示以当前正式版本为基准的修改前后预览。
- 用户批准后按 `currentVersion + 1` 创建新版本，旧版本继续保留；该可选修改不预先绑定 v2。
- “邀请成员”打开模拟分享链接弹窗，但不实现真实账号和加入流程。

**Caption：** 行程是拥有稳定 URL、锁定项和版本记录的产品对象，而不是只存在于聊天中的一次性回答。

### Screen 5：Group Harmony

**路由：** `/trips/penang-demo/consensus`

核心内容：

- 展示四名成员、共同偏好、强需求、冲突、可分组活动和待投票事项。
- 初始正式 Harmony 快照为 72%；运行优化后先展示待批准候选的系统估算 91%，不能直接写入正式指标。
- 显示逐成员满意度、最低成员满意度和“没有成员被忽略”提示。
- 显示造成候选变化的实际活动原因（如替换不适配餐饮、保留购物时段和调整同日活动顺序）。
- 优化必须保留 Sam 的素食需求和 Taylor 的购物需求；逐成员分数标为系统估算，成员真实反馈单独显示，Demo 默认为“未收集”。
- 提供明确的“批准并应用 Harmony 调整”按钮；批准前正式 Harmony 仍为 72%，批准后才更新正式指标并按当前版本递增。
- 不显示任何成员的精确私人预算或敏感偏好原话。
- Harmony 计算本身不创建版本；用户批准实际活动调整并通过硬约束校验后，才更新正式 Harmony 为 91% 并创建下一个版本。

**Caption：** TripMind 在提高群组整体满意度的同时，保护最低满意成员和成员的私密输入。

### Screen 6：Travel Energy

**路由：** `/trips/penang-demo/itinerary?panel=energy`

Travel Energy 按照 `plan.md` 的信息架构作为独立行程页内的面板呈现，不新增正式 `/energy` 路由。

核心内容：

- 展示每日活动数、步行距离、连续活动时间、换乘次数、休息块和疲劳风险。
- Day 2 优化前：步行 8.4 km、0 个休息块、疲劳风险 High；优化后：步行 4.6 km、1 个休息块、疲劳风险 Medium。
- Sam 的 Demo Fixture 每日硬上限为 9.0 km，舒适目标为 ≤5.0 km；8.4 km 通过硬约束但高疲劳风险仍需解释为风险提示，不是违规。
- 说明移动、缩短或替换了哪些活动，以及保留了哪些关键体验。
- 锁定晚餐始终保持不变。
- 批准低步行方案后按批准时的当前正式版本递增；实现只读取批准瞬间的 `currentVersion`，不把 Energy 绑定到某个固定版本号。

**Caption：** TripMind 不只优化景点数量，也把真实步行负担、连续活动和休息需求纳入行程决策。

### Screen 7：暴雨重排与 Experience Diff

**路由：** `/trips/penang-demo/replan`

核心内容：

- 事件模拟器默认触发“Day 2 13:30 暴雨导致 14:00–17:00 户外活动取消”。
- 标记受影响的户外活动和保持不变的同日 19:30 锁定晚餐；候选必须在 17:00 前结束替代活动，重新计算交通并预留 30 分钟预计交通和 60 分钟晚餐缓冲。
- 提供三个候选方案：省钱、保留更多兴趣、最少步行。
- 默认推荐“最少步行”。
- 每个候选展示标准 Experience Diff：
  - Harmony。
  - 最低成员满意度。
  - 预算变化。
  - 步行变化。
  - 疲劳风险。
  - 天气风险。
  - 关键体验保留率。
  - 固定安排影响。
- 选择方案后只生成变更预览，不立即覆盖正式行程。
- 候选草稿以当前正式版本为 `baseVersion`；只有在用户批准且校验通过后，才更新正式版本、正式指标和历史。

**Caption：** TripMind 只修复受影响的行程区间，并解释保留了什么、改变了什么及其代价。

### Screen 8：预算与版本确认

**路由：** `/trips/penang-demo/budget`

核心内容：

- 展示全组 4 人、全程 3 天的 RM4,800 总预算；分类预算为住宿 RM1,900、餐饮 RM1,200、交通 RM700、活动 RM1,000，合计 RM4,800。
- 显示当前正式版本预计花费 RM4,360（1,800 + 1,040 + 540 + 980）和剩余 RM440；暴雨草稿相对当前正式版本 +RM40，预计 RM4,400、剩余 RM400。
- 用当前正式版本、待批准草稿和历史查看状态比较，不写死 Version 1–5 与动作的绑定。
- 三个候选的分类增量、预计总花费和剩余金额按 `plan.md §3.5` 表格渲染：最少步行交通 +RM40（RM4,400/余 RM400）、保留更多兴趣交通 +RM60 且活动 +RM20（RM4,440/余 RM360）、更省预算交通 -RM60（RM4,300/余 RM500）。
- 标准演示中晚起、Harmony 和 Energy 的已批准版本沿用 RM4,360 基准；只有暴雨候选在本 Fixture 中增加 RM40，批准后新正式版本为 RM4,400。若未来候选改变费用，必须从当前正式版本重新计算。
- 展示最终 Experience Diff 和修改原因。
- 用户点击“批准并创建新版本”后按当前正式版本递增；标准八屏路径依次批准晚起修改、Harmony、Energy、暴雨时呈现 Version 1 → 2 → 3 → 4 → 5。
- 历史版本仍可查看，正式版本不会被覆盖；可选的晚起修改若获批会占用下一个版本号并使后续编号顺延。
- 提供 `Reset Demo`，将所有状态恢复到初始场景。

**Caption：** 重大修改只有在用户确认后才生成新版本，预算、影响和历史变化都可以复核。

## 4. 视觉与交互规范

### 4.1 视觉方向

- 风格：清爽旅行助手。
- 背景：浅色中性色。
- 主色：自然青绿色，用于主按钮、导航和积极状态。
- 辅助色：蓝色用于信息，橙色用于提醒，红色仅用于明确风险。
- 不用颜色作为状态的唯一表达；同时提供文字、数字和图标。
- 卡片采用柔和圆角、轻阴影和清晰间距，避免过度控制台化。
- 不依赖外部旅游照片；使用渐变、Lucide 图标和简化路线图降低版权与加载风险。

### 4.2 响应式行为

- Mobile-first 设计，优先验证 390px 宽度。
- 移动端使用底部导航和全宽 Agent 抽屉。
- 桌面端在 1440px 宽度下使用顶部或侧边导航，并允许时间线与指标并排。
- 正文最小 16px，主要触控目标不小于 44×44px。
- 所有核心按钮具有清晰的 hover、active、disabled、loading 和 focus 状态。

### 4.3 状态反馈

- 所有模拟生成过程必须有明确进度，不能只显示无限 spinner。
- 所有估算数据标注来源、更新时间或 Demo 状态。
- 修改、优化和重排必须先展示预览，再允许用户批准。
- 失败状态提供“重试”和“加载 Demo 数据”操作。

## 5. 技术实现计划

### 5.1 技术栈

- Next.js App Router。
- TypeScript。
- Tailwind CSS。
- 少量 shadcn/ui。
- Lucide 图标。
- Vercel 部署。
- Web App Manifest 与 PWA standalone 配置。

### 5.2 Fixture 与状态管理

Prototype 不接真实 Supabase、LLM、地图、天气或地点 Provider。所有数据来自固定槟城 Fixture，并使用确定性的状态变化保证演示稳定。

```ts
type ReplanOption = "budget" | "interest" | "walking";

type PrototypeState = {
  demoStep: 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8;
  mode: "new_user" | "demo_preset";
  currentVersion: number | null;
  selectedReplan: ReplanOption | null;
  pendingDraft: {
    id: string;
    baseVersion: number;
    kind: "harmony" | "energy" | "replan" | "itinerary_change";
    proposal: {
      changes: string[];
      reasons: string[];
      estimatedBudgetDelta: number;
      estimatedMetrics: Record<string, number | string>;
    };
    createdAt: string;
    expiresAt: string;
    status: "pending" | "approved" | "cancelled" | "expired";
  } | null;
  viewedVersion: number | null;
  history: DemoVersion[];
  formalHarmony: number | null;
  estimatedHarmony: number | null;
  memberFeedback: "not_collected" | "collected";
};
```

- 使用 React Context 或轻量 client store 管理跨页面状态。
- 将 Prototype 状态同步到 `localStorage`，支持刷新、浏览器返回和直接打开子路由；当前正式版本、待审批草稿和历史查看状态都必须恢复。
- `Reset Demo` 清除本地状态、草稿和版本历史，回到初始入口；重新点击 Demo 才加载四人预设。恢复后的普通新用户仍为单人。
- 草稿保存 `baseVersion`、候选原因、创建时间和过期时间；当前版本变化或草稿过期时不可批准，必须从当前版本重新生成。
- 取消/跳过只关闭草稿，重复批准返回同一个幂等结果且不重复创建版本；任何成功批准的实际变更都先校验，再将 `currentVersion` 递增 1。`demoStep`（演示步骤）、`currentVersion`（正式版本）、`pendingDraft`（待审批）和 `viewedVersion/history`（历史查看）互相独立。
- 模拟延迟只用于展示 Agent 流程，不依赖网络结果。
- 地图使用静态路线示意，不使用需要 API key 的地图服务。

### 5.3 版本规则

版本规则引用 `plan.md §3.5`：Version 1 是首次生成并通过约束校验的正式行程；之后每次批准实际活动/时间线变更，都以批准时的当前正式版本递增一个版本号。Harmony、Energy、重排和可选的晚起修改都不预先绑定 v2/v3/v4。

Group Harmony 的 `72% → 91%` 在候选阶段是系统估算的偏好匹配度；画面必须同时展示活动调整原因。只有批准实际 Harmony 调整并通过校验后，正式 Harmony 才更新并创建新版本。成员真实反馈另行记录，不能用系统估算冒充。

标准八屏演示若依次批准 Screen 4 的“第二天不要太早”、Harmony、Energy、暴雨，才展示 Version 1 → 2 → 3 → 4 → 5。演示步骤、当前正式版本、待审批草稿和历史查看是四种独立状态：跳过某一演示步骤或可选能力只推进 `demoStep`；取消或关闭草稿不改变当前正式版本；过期草稿要求重新生成；刷新、直接打开子路由和返回都恢复它们；Reset Demo 清除它们并回到初始入口。

## 6. 制作顺序

### Phase 1：基础、锁定保护与暴雨契约

- 初始化 Next.js、TypeScript、Tailwind CSS 和基础组件。
- 建立颜色、字体、间距、圆角、阴影和状态 token。
- 建立响应式 App Shell、Trip 顶部栏和移动端导航。
- 建立槟城 Fixture、Prototype store、持久化和 Reset Demo。
- 先实现 Day 2 19:30 锁定晚餐保护、Day 2 13:30 暴雨事件 fixture、交通/缓冲校验、草稿过期和 `baseVersion` 审批规则。

### Phase 2：首次规划闭环

- 完成首页 Chatbox。
- 完成 Agent 补问与条件提取展示。
- 完成生成前确认和分阶段生成反馈。
- 完成独立行程页与 Version 1。

### Phase 3：修改与突发重排

- 完成“第二天不要太早”的变更预览；版本号按当前版本递增。
- 在首次行程和修改闭环后先完成暴雨重排、候选分类增量和标准 Experience Diff；锁定晚餐、交通和缓冲规则必须可观察。

### Phase 4：Harmony、Energy、预算与审批

- 完成 Harmony 候选的系统估算、活动调整原因、成员真实反馈分层和批准后正式指标更新。
- 完成 Energy 前后比较；统一展示 Day 2 的 8.4 km → 4.6 km、休息 0 → 1。
- 完成预算面板、版本比较和按当前版本递增的审批。

### Phase 5：QA、部署与提交素材

- 完成移动端、桌面端、键盘与字体缩放检查。
- 加入 Manifest、图标、主题色和 standalone 设置。
- 部署 Vercel 并使用隐身窗口验证公开访问。
- 截取 8 个核心画面并写 caption。
- 按第 7.4 节完成 3–5 名参与者的任务式可用性测试，并记录可观察结果。
- 补充 README、竞品对照、ideation board 和 mentor feedback。

## 7. Prototype 验收标准

### 7.1 流程验收

- 用户可以从首页连续完成全部 8 屏演示。
- 8 屏展示顺序固定为“模糊输入 → Agent 补问 → 条件确认与生成 → 独立行程 → Harmony → Energy → 暴雨重排 → 预算与版本确认”；实现顺序可提前建设暴雨/锁定保护，但不得改变展示顺序。
- 主要操作不存在死链、无响应按钮或无法恢复的状态。
- 整套流程可以在 5–6 分钟内稳定完成。
- 无外部 API key、账号或实时网络数据也能完成演示。
- 所有重大修改都先展示预览，再由用户批准。
- 锁定晚餐在所有修改、Energy 优化和暴雨重排中保持不变；暴雨候选必须满足 Day 2 17:00 前结束、预计交通 30 分钟和晚餐前 60 分钟缓冲。

### 7.2 数据验收

- Harmony 候选正确展示系统估算 `72% → 91%`、活动调整原因和“成员真实反馈：未收集”；批准实际调整后才更新正式指标并递增版本。
- 最低成员满意度不会因整体分数提高而下降。
- Energy 正确展示 Day 2 的 `8.4 km → 4.6 km`、`0 → 1` 个休息块和 `High → Medium`；初始 8.4 km 不超过 9.0 km 硬上限，High 仅为风险提示。
- 重排页面显示三个方案及完整 Experience Diff。
- 预算总额、分类预算、预计花费和剩余预算前后一致：总额/分类合计 RM4,800，当前 RM4,360/余 RM440，暴雨草稿 +RM40 为 RM4,400/余 RM400。
- 版本以批准时的当前正式版本递增；标准演示全部批准时呈现 Version 1 → 2 → 3 → 4 → 5，但动作不得硬编码到固定编号，旧版本不会被覆盖。

### 7.3 设备与无障碍验收

- 在 390px 手机宽度和 1440px 桌面宽度完成完整流程。
- 刷新、浏览器返回和直接打开子路由后状态可以恢复。
- 主要操作可以使用键盘完成，焦点清楚可见。
- 表单具有标签、错误提示和 loading/disabled 状态。
- 字体放大后不遮挡核心操作。
- 状态同时使用文字、数字或图标表达，不只依赖颜色。

### 7.4 任务式可用性测试（3–5 人）

在发布前邀请 3–5 名未参与实现的参与者，使用同一公开 Prototype 链接完成以下任务；观察者只记录完成/失败、是否走错入口、是否读懂状态和口头解释，不提示操作路径。

| 任务 | 可观察验收 |
| --- | --- |
| 1. 从“我想去旅行”开始生成行程 | 参与者能找到 Chatbox，不先创建 Plan；能说出普通新用户默认为 1 人 |
| 2. 查看并批准 Harmony 候选 | 能指出 72% → 91% 是系统估算，能说出至少一个活动调整原因，并知道批准前不是正式指标 |
| 3. 检查 Energy 负荷 | 能定位 Day 2 的 8.4 → 4.6 km、0 → 1 休息块；能理解 High 是风险提示且未超过 9.0 km 硬上限 |
| 4. 处理 Day 2 暴雨 | 能找到受影响的下午户外活动，确认 19:30 晚餐未变，并在批准前查看交通/缓冲和 Experience Diff |
| 5. 取消或跳过草稿后刷新并查看历史 | 草稿取消/跳过后正式版本不变；刷新或直接打开子路由仍能看到当前版本与历史，重复批准不产生重复版本 |

通过条件：至少 80% 的参与者（按人数向上取整）独立完成任务 1–4，且每项任务至少 80% 的参与者正确识别当前正式版本、待批准草稿和历史状态；所有参与者都能完成刷新/返回或明确指出恢复入口。失败步骤记录在提交材料中并在发布前修复。

### 7.5 发布验收

- Vercel HTTPS 链接在无登录隐身窗口可打开。
- 所有 Prototype 路由均可直接访问。
- Web App Manifest、图标、`start_url` 和 `display: standalone` 有效。
- README 中包含公开链接、8 张截图和对应 caption。

## 8. 配套文档

README 或 `docs/ideation.md` 需要同步准备。三份规划文档的职责保持分离：`plan.md` 维护最终 MVP 的产品范围和唯一规则基线；本文件维护 Prototype 本阶段的 8 屏范围、演示脚本和验收；`prototype.md` 维护画面布局、组件和交互状态。预算、事件日期、步行/Harmony 指标和版本语义均引用 `plan.md §3.5`，不要在本节或画面文档另写第二套规则。

1. **现有产品与不足**
   - 至少实际体验并核实 Wanderlog。
   - 对照行程组织、群组偏好、预算、体力和突发重排。
   - 说明 TripMind 在最低满意度保护、Travel Energy、锁定项、局部重排和 Experience Diff 上的区别。

2. **创意保留与放弃记录**
   - 使用 `Idea | Kept / Dropped | Reason | Resulting Change` 表格。
   - 记录 Chatbox-first、Harmony、Energy、Replan、Experience Diff 和 Fixture fallback。
   - 记录交易、原生 App、实时位置、复杂多目的地优化、完整离线编辑和多个真实 Provider 被延期的原因。

3. **Ideation Board**
   - 包含 Problem Tree、用户流程和功能取舍。
   - 保留被放弃的方向，证明方案经过迭代。

4. **Mentor Feedback**
   - 使用 `Date | Mentor | Feedback Received | What Was Changed` 表格。
   - 未采纳的反馈同样记录原因。

5. **Prototype 展示**
   - 公开链接。
   - 8 个画面截图。
   - 每屏 caption。
   - 5–6 分钟演示顺序。
   - 使用的依赖、模板、图标、来源和许可证。

## 9. 本阶段明确不做

- 不接真实 Supabase、Auth 或数据库 migration。
- 不接真实 AI、天气、地点、路线或地图 Provider。
- 不实现真实邀请加入、多人协作或权限系统。
- 不实现真实服务端 Business Scope Guard，只模拟允许和拒绝结果。
- 不实现 Admin、Surprise Me、导出或手动 Plan 表单的完整页面。
- 不实现离线编辑、background sync 或 push notification。
- 不把 Prototype 宣称为已达到 `plan.md` 中完整 MVP 的功能和安全验收。

以上能力继续保留在最终 MVP 范围中，但不阻塞本次 Prototype 的设计、演示和提交。
