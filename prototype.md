# TripMind 前端产品设计与交互原型

> 版本：Prototype v1  
> 设计目标：用一条清晰、可解释、可审批的体验，把用户从“我想去旅行”带到可执行的槟城 3 天行程。  
> 设计依据：`plan.md`、`prototype-plan.md`、`dataflow.png`。  
> 实现范围：Next.js App Router + TypeScript + Tailwind CSS + Lucide；使用固定槟城 Demo Fixture，不依赖外部 API。

> 文档职责：`plan.md §3.5` 是预算、指标、事件、约束和版本语义的唯一规则来源；本文件只描述 8 个画面的布局、文案和交互映射，范围与验收见 `prototype-plan.md`。

## 1. 产品体验方向

### 1.1 核心叙事

TripMind 不是生成一段旅游文案，而是帮助用户保护整趟旅程的体验：

> **Don’t just plan the trip. Protect the experience.**

前端需要让用户持续看见四件事：

- Agent 理解了什么，以及还缺什么。
- 行程为什么这样安排，哪些数据只是估算。
- 修改会影响什么，用户需要付出什么代价。
- 哪些固定安排和成员限制始终被保护。

### 1.2 设计原则

1. **Chatbox-first**：首页先让用户说想法，不先要求创建 Plan 或填写长表单。
2. **Progressive disclosure**：先显示最必要的信息，预算、Harmony、Energy 和重排作为行程生成后的增强面板。
3. **Explain before apply**：任何重大修改先显示差异预览，用户批准后才创建新版本。
4. **Facts versus estimates**：已确认、估算、Demo 数据和待确认状态使用文字标签区分，不能只依赖颜色。
5. **Protect the outlier**：整体 Harmony 提升不能牺牲最低满意成员；锁定晚餐不能被自动修改。
6. **Calm over dashboard**：使用旅行感的留白、卡片和时间线，避免把产品做成密集的后台控制台。

## 2. 视觉设计系统

### 2.1 视觉关键词

清爽、可靠、有一点旅途感、可解释、温和而不幼稚。画面不依赖旅游照片，以渐变、路线曲线、地图节点和 Lucide 图标建立识别度。

### 2.2 色彩 Token

| Token | 值 | 用途 |
| --- | --- | --- |
| `--bg` | `#F7F8F5` | 页面背景，略带暖色的浅灰 |
| `--surface` | `#FFFFFF` | 卡片、抽屉和弹窗 |
| `--ink` | `#17332F` | 主标题和重要数字 |
| `--muted` | `#6C7D78` | 次要说明、时间和来源 |
| `--teal-700` | `#0E7168` | 主按钮、导航选中、主品牌色 |
| `--teal-100` | `#DDF2ED` | 主品牌浅底、成功背景 |
| `--blue-600` | `#3678B8` | 信息提示、来源和路线信息 |
| `--amber-600` | `#C78324` | 估算、提醒、待确认 |
| `--coral-600` | `#C95E4B` | 风险、暴雨、冲突；只用于明确风险 |
| `--line` | `#E2E9E5` | 分割线和卡片边框 |
| `--locked` | `#5B5B72` | 锁定安排，配合锁图标和文字 |

状态表达必须同时包含文字、数字或图标：

| 状态 | UI 表达 |
| --- | --- |
| Confirmed | `已确认` + Check 图标 + `teal` |
| Estimated | `估算` + Info 图标 + `amber` |
| Demo data | `Demo data` 标签 + Database 图标 |
| Locked | `已锁定` + Lock 图标 + 紫灰色 |
| Needs approval | `待批准` + Clock 图标 + `amber` |
| Risk | `风险` + AlertTriangle 图标 + `coral` |

### 2.3 字体、间距和形状

- 字体：优先 `Inter, Noto Sans SC, system-ui, sans-serif`；不依赖必须联网的字体文件。
- 正文最小字号 `16px`；辅助文字不低于 `13px`。
- 标题：`32/40`（桌面）、`26/34`（移动）；卡片标题 `18/26`。
- 间距采用 4px 基准：`4 / 8 / 12 / 16 / 24 / 32 / 48`。
- 小卡片圆角 `12px`，主要容器 `20px`，按钮 `12px`；避免所有元素都使用胶囊形。
- 卡片阴影：`0 8px 30px rgba(23, 51, 47, 0.06)`；风险和弹窗不使用夸张阴影。
- 主要触控目标不小于 `44 × 44px`。

### 2.4 组件状态

所有主要按钮、输入框和卡片都需要实现以下状态：

`default → hover → active → focus-visible → disabled → loading → success / error`

Focus 使用 `2px` 的 `--teal-700` 外描边，并保留 `2px` 间隔。Loading 按钮显示动作动词，例如“正在生成行程”，不使用无限旋转图标替代文案。

## 3. 信息架构和路由

| 路由 | 画面 | 主要任务 |
| --- | --- | --- |
| `/` | Home / AI Chatbox | 输入模糊旅行想法，加载 Demo |
| `/chat/demo` | Agent Clarifier | 补齐目的地和日期/天数 |
| `/chat/demo?stage=ready` | Requirement Review | 确认条件并生成 |
| `/trips/penang-demo/itinerary` | Itinerary | 查看和修改当前行程 |
| `/trips/penang-demo/consensus` | Group Harmony | 查看群组协调和优化结果 |
| `/trips/penang-demo/itinerary?panel=energy` | Travel Energy Panel | 查看并批准低步行方案 |
| `/trips/penang-demo/replan` | Disruption Replanner | 选择暴雨后的局部重排方案 |
| `/trips/penang-demo/budget` | Budget & Version Approval | 比较预算、差异并创建新版本 |

增强能力不应破坏主路径。顶部或移动底部导航始终保留“行程、Harmony、预算”入口；Energy 作为行程内面板打开，避免制造一个与行程脱节的孤立页面。

## 4. 全局 App Shell

### 4.1 桌面布局（≥ 1024px）

```text
┌────────────────────────────────────────────────────────────────────┐
│ TripMind        我的行程      Harmony      预算       [Reset Demo] │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│                         页面内容 max-width 1200                    │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

- Header 高度 `72px`，背景半透明白色，滚动时保持顶部。
- 首页 Header 只显示品牌、`加载槟城 Demo` 和帮助入口，避免用户一打开就看到过多产品结构。
- 行程页 Header 显示 Trip ID、当前版本和 `已保存` 状态。
- 页面内容最大宽度 `1200px`；主内容与侧栏采用 `minmax(0, 1fr) / 320px`。

### 4.2 移动布局（< 768px）

```text
┌──────────────────────────┐
│ TripMind       ⋯         │
├──────────────────────────┤
│ 页面内容                 │
│                          │
├──────────────────────────┤
│ 行程  Harmony  预算  对话 │
└──────────────────────────┘
```

- 使用固定底部导航，底部安全区至少 `16px`。
- Agent 抽屉从底部出现，占屏幕高度不超过 `92vh`，支持下滑关闭。
- 桌面双栏在移动端改为纵向堆叠；指标卡片两列，宽度不足时改为一列。
- 时间线不做横向滚动；每个时间块改为垂直卡片，移动时间以连接线展示。

## 5. 固定 Demo 场景

用于所有画面和演示录屏的固定数据：

| 字段 | 值 |
| --- | --- |
| Trip | Penang Food & Culture Escape |
| Trip ID | `TRP-PEN-2403` |
| 目的地 | 槟城，马来西亚 |
| 时长 | 3 天 |
| 成员 | Alex、Jamie、Sam、Taylor（点击“加载槟城 Demo”后载入的 4 人预设） |
| 总预算 | RM 4,800（全组 4 人、全程 3 天） |
| 分类预算 | 住宿 RM 1,900；餐饮 RM 1,200；交通 RM 700；活动 RM 1,000（合计 RM 4,800） |
| 当前正式版本预计 | RM 4,360（住宿 1,800 + 餐饮 1,040 + 交通 540 + 活动 980），剩余 RM 440 |
| 暴雨草稿预计 | 当前正式版本 +RM 40 = RM 4,400，剩余 RM 400 |
| 共同兴趣 | 美食、文化 |
| Demo 输入来源 | Sam 素食；Sam 每日步行硬上限 9.0 km、舒适目标 ≤5.0 km；Taylor 希望保留购物时间；均为 `Demo Fixture preset`，不是系统猜测 |
| 固定安排 | Day 2 19:30 已锁定晚餐 |
| 暴雨事件 | Day 2 13:30 触发，影响 14:00–17:00 户外活动；替代活动须 17:00 前结束，预留预计交通 30 分钟与晚餐前 60 分钟缓冲 |
| Harmony | 待批准候选的系统估算 `72% → 91%`；批准实际活动调整后才更新正式指标 |
| Energy | Day 2 步行 `8.4 km → 4.6 km`；休息 `0 → 1`；疲劳 `High → Medium`（系统估算） |

普通新用户的默认人数是 1 人；只有主动加载 Demo 才出现四人及上述限制。所有金额、路线、天气、Harmony/Energy 和成员满意度在 Prototype 中都标记为 `Estimated` 或 `Demo data`，成员真实反馈单独显示（Demo 为“未收集”），不伪装成实时信息。

## 6. 八个核心画面

### Screen 1 — 首页 AI Chatbox

**路由：** `/`

```text
┌──────────────────────────────────────────────────────────────┐
│ TripMind                                      加载槟城 Demo │
│                                                            │
│        先说说你想去哪里。计划交给我。                        │
│        从一个模糊想法开始，我会帮你补齐必要信息。              │
│                                                            │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 我想去旅行，但还没有任何计划                         ↑ │ │
│ └────────────────────────────────────────────────────────┘ │
│ 试试：我想去海边 · 帮我安排 3 天美食之旅 · 加入槟城 Demo      │
│                                                            │
│ 最近行程                                                   │
│ ┌──────────────────────────┐                                │
│ │ Penang Food & Culture    │ 继续规划 →                     │
│ │ Version 1 · 3 天         │                                │
│ └──────────────────────────┘                                │
└──────────────────────────────────────────────────────────────┘
```

交互规则：

- Chatbox 是首屏第一视觉，输入框默认获得 focus。
- 提交模糊输入后显示一条用户消息，再进入 `/chat/demo`；模拟范围检查时显示 `正在理解你的旅行想法…`。
- 首次进入先按普通新用户显示 1 人单人状态；点击 `加载槟城 Demo` 是明确的用户动作，之后才载入四人 `Demo Fixture preset` 和 Sam/Taylor 预设限制，保留模糊输入到补问的演示开场。
- 非旅行请求展示固定引导，不创建任何状态：

  > 我只负责 TripMind 的旅行规划与行程管理。你可以告诉我目的地、日期，或者想怎样修改现有行程。

**Caption：** 用户不需要先创建 Plan，只要说出模糊想法即可开始规划。  
**验证目标：** 用户是否理解 Chatbox 是主要入口，而不是把 TripMind 当成传统表单工具。

### Screen 2 — Agent 补问

**路由：** `/chat/demo`

页面结构：左侧或上方显示对话，右侧或下方显示 `我已记住` 条件卡。

```text
┌──────────────────────────────┬───────────────────────────────┐
│ TripMind Agent               │ 当前条件                       │
│                              │                               │
│ 你：我想去旅行，但还没有计划 │ 目的地     待确认              │
│                              │ 天数       待确认              │
│ Agent：当然。先确认目的地和  │ 人数       1 人（默认单人）     │
│ 旅行时长，其他可以稍后调整。  │ 兴趣       未指定               │
│                              │ 节奏       未指定               │
│ [槟城，3 天，四个人，喜欢美食，│                               │
│  不想太赶]（Demo 预设快捷回复）│ 还需要 目的地 + 日期/天数      │
│                              │                               │
│ ┌──────────────────────────┐ │                               │
│ │ 输入回复……              ↑ │ │                               │
│ └──────────────────────────┘ │                               │
└──────────────────────────────┴───────────────────────────────┘
```

交互规则：

- Agent 每轮最多追问 1–2 个阻止生成的问题，优先目的地和日期/天数。
- 快捷回复必须是可点击按钮；点击后进入 `ready_to_generate`，不再重复提问。四人及限制只来自用户主动加载的 `Demo Fixture preset`。
- 普通新用户的默认人数为 1 人；`你决定` 只对实际采用的可选字段显示 `系统默认`，不把人数、饮食或步行限制写成系统猜测。
- 用户已经提供的信息以 chip 显示，支持删除单项后重新补问。
- 侧栏在移动端变成可折叠的 `当前条件` 抽屉。

**Caption：** Agent 只补问会阻止行程生成的问题，并保留已经提供的信息。  
**验证目标：** 用户是否感到是在被协助，而不是被一张长表单盘问。

### Screen 3 — 条件确认与生成

**路由：** `/chat/demo?stage=ready`

```text
┌──────────────────────────────────────────────────────────────┐
│ 生成前确认                                      Demo data    │
│                                                            │
│ 这是我对这趟旅行的理解                                      │
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                 │
│ │ 槟城   │ │ 3 天   │ │ 4 人   │ │ 美食文化│                 │
│ └────────┘ └────────┘ └────────┘ └────────┘                 │
│                                                            │
│ 可选默认：不太赶 · 活动分类预算 RM 1,000                       │
│ Demo 预设：4 人 · Sam 素食/步行上限 · Taylor 购物时间          │
│ 这些信息之后都可以修改；预设来源不会伪装成系统猜测。           │
│                                                            │
│                  [返回补充]  [生成 3 天行程]                 │
└──────────────────────────────────────────────────────────────┘
```

点击生成后，按钮和进度区依次显示：

1. `理解需求` — 已读取目的地、天数、人数和偏好。
2. `寻找活动候选` — 使用槟城 Demo data。
3. `估算路线与费用` — 费用和移动时间标记为 Estimated。
4. `执行硬约束校验` — 检查 Day 2 19:30 锁定晚餐、素食、9.0 km 步行硬上限和时间/交通约束。
5. `保存 Version 1` — 生成独立行程。

每一步有时间线图标、短说明和完成状态；不使用只有 spinner 的等待页。完成后自动进入独立行程页。

**Caption：** 结构化条件在保存前经过约束校验，用户能看到系统如何从需求走到行程。  
**验证目标：** 用户是否信任生成过程，并理解结果不是未经检查的聊天文本。

### Screen 4 — 独立行程页

**路由：** `/trips/penang-demo/itinerary`

```text
┌──────────────────────────────────────────────────────────────┐
│ ← 我的行程   Penang Food & Culture Escape   当前 Version {n} · 已保存│
│ 槟城 · 3 天 · 4 人（Demo 预设） [邀请成员] [Agent]             │
├──────────────────────────────────────────────────────────────┤
│ [行程] [Harmony 72%] [Energy] [预算] [暴雨重排]               │
│                                                            │
│ Day 1 · 抵达与乔治市文化                    RM 260 Estimated │
│ 10:00  抵达槟城机场                                        │
│ 12:00  乔治市老街午餐                  交通 20 min          │
│ 14:00  壁画与街区漫步                  步行 2.2 km          │
│ 18:30  休息块                         建议 45 min          │
│                                                            │
│ Day 2 · 市场、下午户外段与已锁定晚餐                         │
│ 09:30  槟城中央市场                                      │
│ 14:00  户外文化活动                      14:00–17:00       │
│ 19:30  海墘饭店晚餐                      已锁定 · 4 人       │
│                                                            │
│              [问 Agent 修改行程]                            │
└──────────────────────────────────────────────────────────────┘
```

核心模块：

- `TripHeader`：Trip ID、版本、保存状态、Demo data 标记和邀请入口。
- `DayTimeline`：时间、地点、移动时间、活动标签、费用和来源。
- `LockedActivity`：锁图标、`已锁定` 文字和不可被自动覆盖的视觉样式。
- `AgentComposer`：支持输入“第二天不要太早”，提交后打开修改预览。
- `MetricsRail`：Harmony、Energy、预算三个摘要卡；移动端放在时间线下方。

修改流程：

1. 用户输入“第二天不要太早”。
2. Agent 显示 `当前正式版本 → 待批准草稿` 的预览：开始时间 `09:30 → 10:30`，午后活动缩短 30 分钟，Day 2 19:30 锁定晚餐不变。
3. 预览底部显示 `[保留修改] [继续调整]`。
4. 用户批准后以批准时的 `currentVersion + 1` 创建新版本；顶部版本选择器可查看历史版本，旧版本不覆盖。若跳过或取消，正式版本不变。

**Caption：** 行程是有稳定 URL、锁定项和版本记录的产品对象，而不是只存在于聊天中的回答。  
**验证目标：** 用户是否知道当前版本、变化范围和可以继续执行的下一步。

### Screen 5 — Group Harmony

**路由：** `/trips/penang-demo/consensus`

```text
┌──────────────────────────────────────────────────────────────┐
│ Group Harmony                  正式 72% · 候选估算 91%       │
│ 找到大家都能接受的旅行节奏（待批准）                          │
│                                                            │
│  Harmony Score       最低成员满意度          成员             │
│      72%                   64%                4 人            │
│                                                            │
│ ┌──────────────┐ ┌──────────────┐ ┌────────────────────────┐ │
│ │ 全员共识      │ │ 需要协调      │ │ 成员满意度              │ │
│ │ 美食 · 文化   │ │ 购物时间      │ │ Alex 92  Jamie 84      │ │
│ │              │ │ 素食替代      │ │ Sam 64   Taylor 78      │ │
│ └──────────────┘ └──────────────┘ └────────────────────────┘ │
│                                                            │
│ [运行 Harmony 优化]  [查看活动调整原因]  [批准并应用调整]      │
└──────────────────────────────────────────────────────────────┘
```

优化后：

- Overall Harmony `72% → 91%`；91% 是待批准候选的系统估算偏好匹配度，正式值仍为 72%。
- 逐成员满意度和最低成员满意度并排展示。
- 明确提示：`Sam 的素食需求已保留`、`Taylor 的购物时间已保留`、`没有成员被忽略`；显示替换餐饮、保留购物时段和调整活动顺序等实际原因。
- 逐成员分数和 72%/91% 都标为 `系统估算`；`成员真实反馈：未收集` 单独展示，不以估算代替成员原话。
- Harmony 运行只产生待批准候选；用户批准实际时间线变化且校验通过后，正式 Harmony 才更新为 91% 并创建下一个版本。跳过/取消不改变正式指标。
- 不显示成员精确私人预算或敏感偏好原话，只显示聚合结论。

**Caption：** TripMind 同时优化整体满意度和最低成员满意度，保护少数成员的硬需求。  
**验证目标：** 用户是否能看懂“更公平”而不仅是“分数更高”。

### Screen 6 — Travel Energy 面板

**路由：** `/trips/penang-demo/itinerary?panel=energy`

```text
┌──────────────────────────────────────────────────────────────┐
│ Travel Energy                                      [关闭]     │
│ 目标：减少体力负担，同时保留美食和文化体验                   │
│                                                            │
│             Day 2 初始      Day 2 候选      变化              │
│ 步行        8.4 km          4.6 km          -3.8 km          │
│ 休息块      0               1               +1               │
│ 疲劳风险    High            Medium          ↓                │
│ 换乘        5               3               -2               │
│                                                            │
│ 调整说明                                                     │
│ • 将 Day 2 户外段与市场按地理位置/交通重新排序                 │
│ • 缩短连续步行，加入 45 分钟休息                             │
│ • 保留 Day 2 19:30 已锁定晚餐，并校验交通缓冲                 │
│                                                            │
│                    [批准低步行方案]                          │
└──────────────────────────────────────────────────────────────┘
```

- 面板从行程页打开，桌面端为右侧抽屉，移动端为底部 Sheet。
- 以前后对比为主，不把复杂评分公式暴露给用户。
- 8.4 km 和 4.6 km 都是 Day 2 的系统估算；Sam 的 Demo Fixture 每日硬上限是 9.0 km，舒适目标是 ≤5.0 km。初始 8.4 km 通过硬约束，但 `High` 只表示风险提示，不等于违规。
- 批准后按当前正式版本递增，顶部显示 `Version {currentVersion} · Energy 优化`；Energy 不绑定任何固定版本号。
- 如果用户关闭面板，不改变行程状态。

**Caption：** 行程不仅优化景点数量，还把步行、连续活动和休息需求纳入决策。  
**验证目标：** 用户是否理解低步行方案保留了关键体验，而不是简单删掉活动。

### Screen 7 — 暴雨重排与 Experience Diff

**路由：** `/trips/penang-demo/replan`

```text
┌──────────────────────────────────────────────────────────────┐
│ 暴雨影响了 Day 2 下午户外活动              Demo weather event │
│ 13:30 事件 · 受影响 14:00–17:00 户外段                       │
│ 保持不变：Day 2 19:30 已锁定晚餐                             │
│                                                            │
│ 推荐方案                                                   │
│ ┌────────────┐ ┌────────────┐ ┌────────────┐                │
│ │ 最少步行   │ │ 保留兴趣   │ │ 更省预算   │                │
│ │ 推荐       │ │            │ │            │                │
│ │ +RM 40     │ │ +RM 80     │ │ -RM 60     │                │
│ └────────────┘ └────────────┘ └────────────┘                │
│                                                            │
│ Experience Diff · 最少步行                                  │
│ Harmony        91%* → 89%*       最低满意度 78%* → 78%*       │
│ 预算           RM4,360 → 4,400   步行       Day 2 -1.6 km    │
│ 疲劳风险       Medium → Low      天气风险   High → Low        │
│ 关键体验保留   88%*              固定安排   无影响           │
│                                                            │
│              [查看变更]  [选择并继续审批]                    │
└──────────────────────────────────────────────────────────────┘
```

交互规则：

- 默认选择“最少步行”，但不自动应用；Harmony/满意度/体验保留率的星号表示系统估算，成员真实反馈另行显示。
- 候选卡片必须同时显示方案标签、预算变化、步行变化和适用理由。
- `查看变更` 展开逐活动 diff：新增、移动、替换、取消分别使用图标和文字。
- 选择方案后只生成以当前正式版本为 `baseVersion` 的待审批草稿；不得直接覆盖当前正式版本。候选必须在 17:00 前结束替代活动，并有 30 分钟预计交通和 60 分钟到晚餐缓冲。
- 所有方案都显示固定安排影响，锁定晚餐必须为 `无影响`。
- 用户批准前关闭、跳过或取消草稿不改变正式版本；若刷新后草稿过期或当前版本已变化，必须重新生成。

候选预算必须显示分类增量，避免把 +RM40 误算到已接近上限的活动分类：

| 候选 | 分类变化 | 预计总花费 | 剩余 |
| --- | --- | ---: | ---: |
| 当前正式版本 | 住宿 1,800；餐饮 1,040；交通 540；活动 980 | RM4,360 | RM440 |
| 最少步行（推荐） | 交通 +RM40（540 → 580） | RM4,400 | RM400 |
| 保留更多兴趣 | 交通 +RM60；活动 +RM20（活动 980 → 1,000） | RM4,440 | RM360 |
| 更省预算 | 交通 -RM60（540 → 480） | RM4,300 | RM500 |

**Caption：** TripMind 只修复受影响的区间，并解释保留了什么、改变了什么及其代价。  
**验证目标：** 用户是否能在几秒内比较取舍，而不是重新阅读整份行程。

### Screen 8 — 预算与版本确认

**路由：** `/trips/penang-demo/budget`

```text
┌──────────────────────────────────────────────────────────────┐
│ 预算与版本确认                  待批准 · 当前 Version {n} → 下一版本│
│                                                            │
│ 全组全程总预算 RM 4,800       正式余 RM 440 · 草稿余 RM 400  │
│ [██████████████████░░░] 正式 RM 4,360 → 草稿 RM 4,400       │
│                                                            │
│ 住宿        RM 1,800 / 1,900       餐饮     RM 1,040 / 1,200 │
│ 交通        RM   540 → 580 / 700   活动     RM   980 / 1,000 │
│                                                            │
│ 版本比较                                                     │
│ 当前正式版本 · 待批准草稿 · 历史版本（按批准时动态编号）      │
│                                                            │
│ 最终 Experience Diff                                         │
│ +RM 40（4,360 → 4,400）· Day 2 -1.6 km · 疲劳 Low · 晚餐无影响│
│                                                            │
│ [返回重排]                     [批准并创建新版本]             │
└──────────────────────────────────────────────────────────────┘
```

- 预算卡显示分类预算、预计花费和剩余金额；金额全部显示币种。
- 版本比较用横向滚动表或移动端纵向卡片；每个版本可查看详情。
- Demo 中晚起、Harmony 和 Energy 的已批准版本沿用 RM4,360 费用基准；暴雨草稿展示从当前正式版本到 RM4,400 的 +RM40 分类增量，批准后新正式版本沿用 RM4,400。任何费用变更都从批准时的当前正式版本重新计算。
- 点击批准后以 `currentVersion + 1` 生成新版本，并显示成功 Toast：`Version {n} 已创建，旧版本仍可查看`；标准八屏路径依次批准晚起修改、Harmony、Energy、暴雨时呈现 Version 1 → 2 → 3 → 4 → 5。
- `Reset Demo` 清除本地状态、待审批草稿和历史，回到初始入口；刷新或直接打开子路由恢复当前正式版本、草稿和历史。

**Caption：** 重大修改在创建新版本前展示预算、影响和历史变化，用户可以复核后再批准。  
**验证目标：** 用户是否清楚自己批准了什么，以及旧版本是否仍然可追溯。

## 7. 交互状态机

### 7.1 Prototype Stage

```text
initial
  ↓ submit idea
clarifying
  ↓ destination + date/duration complete
ready_to_generate
  ↓ click generate
generating
  ↓ validation passed
itinerary_ready
  ├─ propose late-start change → pendingDraft(baseVersion=currentVersion)
  ├─ run Harmony               → pendingDraft(kind=harmony, estimated)
  ├─ open Energy               → pendingDraft(kind=energy, estimated)
  └─ propose rain replan       → pendingDraft(kind=replan, estimated)
                                      ↓ approve + validator
                              currentVersion = currentVersion + 1
                                      ↓
                             history + formal metrics updated
```

每一项批准都读取批准瞬间的 `currentVersion`，不得把按钮动作写死为 v2、v3 或 v4。标准八屏演示依次批准晚起修改、Harmony、Energy、暴雨时会显示 Version 1 → 2 → 3 → 4 → 5；跳过或取消时按实际批准次数顺延。Harmony 的 72% → 91% 在批准前只属于系统估算，成员真实反馈单独记录。

### 7.2 用户可见的状态反馈

| 状态 | 页面反馈 |
| --- | --- |
| Loading | 显示当前阶段动词和预计下一步，不显示空白页面 |
| Empty | 解释为什么为空，并提供 `加载槟城 Demo` |
| Error | 显示错误原因、`重试` 和 `加载 Demo 数据` |
| Unsaved preview | 顶部显示 `待批准`，操作区保留 `返回` |
| Success | Toast + 版本号 + 下一步按钮 |
| Out of scope | 固定引导文案，不改变任何旅行状态 |

交互状态处理：跳过某一演示步骤或可选能力只推进 `demoStep`，不改变正式版本；取消或关闭草稿只关闭预览；重复批准返回原审批结果，不创建重复版本；草稿过期或 `baseVersion` 落后时禁止批准并提示重新生成；刷新、浏览器返回和直接打开子路由恢复当前正式版本、待批准草稿和历史；`Reset Demo` 清除全部本地状态并回到初始入口。

## 8. 组件清单

```text
AppShell
├─ TopNav / MobileBottomNav
├─ DemoBanner
├─ ChatComposer
├─ ConversationThread
├─ RequirementChips
├─ GenerationProgress
├─ TripHeader
├─ VersionSelector
├─ DayTimeline
│  ├─ TimelineActivity
│  ├─ TravelSegment
│  └─ LockedActivity
├─ MetricCard
├─ HarmonyPanel
│  ├─ HarmonyScore
│  ├─ MemberSatisfactionList
│  └─ PreferenceSummary
├─ EnergyDrawer
├─ ReplanOptionCard
├─ ExperienceDiff
├─ BudgetSummary
├─ VersionCompare
├─ ApprovalBar
├─ InviteMemberDialog
└─ ResetDemoDialog
```

组件实现要求：

- 所有可交互组件提供 `aria-label` 或可见标签；图标按钮不允许只有图标无名称。
- `MetricCard` 同时接收 `label`、`value`、`delta`、`status` 和 `source`，避免在页面中手写不一致的指标样式。
- `ExperienceDiff` 统一用于修改预览和暴雨重排，字段固定为 Harmony、最低满意度、预算、步行、疲劳、天气、关键体验、固定安排。
- `ApprovalBar` 在桌面端固定在内容底部，在移动端使用安全区内的 sticky footer。
- 所有 Drawer、Dialog 和 Sheet 支持 Escape 关闭、焦点回收和键盘导航。

## 9. 状态与本地持久化

Prototype 使用 React Context 或轻量 client store：

状态字段与审批语义统一引用 `prototype-plan.md §5.2` 的 `PrototypeState`：包含独立的 `demoStep`、`currentVersion`（生成前为 `null`）、`pendingDraft`（含 id、内容、`baseVersion` 与过期时间）、`viewedVersion/history`、正式/估算 Harmony 和成员反馈来源；本文件只说明它们在画面上的呈现。

- Key：`tripmind-prototype-state-v1`。
- 普通新用户初始化为 1 人；只有点击“加载槟城 Demo”才加载四人 `Demo Fixture preset`。解析失败或 schema 版本不匹配时恢复默认入口状态。
- 所有子路由读取同一状态，刷新、浏览器返回和直接打开 URL 不能丢失当前正式版本、待审批草稿或历史查看状态。
- 草稿保存 `baseVersion`、创建/过期时间和活动调整原因；批准前若版本落后或已过期，必须重新生成。成功批准前先通过锁定晚餐、时间、交通、步行上限和饮食约束校验。
- 重复批准必须幂等；跳过、关闭或取消不改变正式版本；`Reset Demo` 清除本地状态、草稿和历史并回到初始入口。
- 生成、优化和批准使用确定性模拟延迟；测试环境可以通过 query 参数跳过延迟。
- `Reset Demo` 必须要求一次轻量确认，确认后清除本地状态并回到首页或当前行程首页。

## 10. 响应式与无障碍验收

### 10.1 断点

| 断点 | 行为 |
| --- | --- |
| `< 640px` | 单列；底部导航；抽屉变 Bottom Sheet；指标一列 |
| `640–1023px` | 主内容单列；可使用双列指标；顶部导航简化 |
| `≥ 1024px` | 桌面 Header；主内容与辅助栏并排；Energy 使用侧抽屉 |
| `≥ 1440px` | 内容保持 `1200px` 最大宽度，增加两侧留白，不拉伸卡片 |

### 10.2 无障碍要求

- 页面有唯一 `h1`，标题层级连续。
- Chatbox、快捷回复、批准按钮可以只用键盘完成。
- 生成阶段使用 `aria-live="polite"` 播报当前阶段；错误使用 `role="alert"`。
- 颜色对比符合 WCAG AA；风险不只用红色，必须有文字和图标。
- 版本、预算、Harmony 和 Energy 数字在屏幕阅读器中有完整语义，例如“Day 2 步行距离，系统估算优化前 8.4 公里，优化候选 4.6 公里，减少 3.8 公里；Sam 硬上限 9.0 公里”。
- 字体放大到 200% 时不隐藏审批按钮，不用固定高度截断重要内容。

## 11. 前端实现顺序

1. 建立 tokens、`AppShell`、导航、按钮、卡片、Badge、Drawer 和 Dialog。
2. 建立槟城 Fixture、Prototype store、localStorage persistence、Day 2 暴雨事件与锁定晚餐保护、交通/缓冲校验和 Reset Demo。
3. 实现首页、补问、条件确认和生成进度，先闭环到 Version 1。
4. 实现行程时间线、动态版本/草稿审批和可选 Agent 修改预览。
5. 在首次行程和晚起修改闭环后先完成暴雨重排、Experience Diff 和锁定保护的可观察路径。
6. 实现 Harmony 系统估算、实际活动调整原因、成员反馈分层和 Energy 面板，再接预算与按当前版本递增的审批。
7. 在 390px、768px、1024px、1440px 检查布局、焦点、折行、底部操作栏和直接路由访问。

## 12. Prototype 验收清单

- [ ] 首页打开即见 Chatbox，不需要先创建 Plan。
- [ ] 可以从模糊输入连续走完 8 个核心画面。
- [ ] Agent 不重复追问已提供的信息；缺少必要字段时才补问。
- [ ] 生成过程有 5 个明确阶段，完成后进入独立行程页。
- [ ] 行程页有稳定 Trip ID、版本号、锁定晚餐、估算标签和 Agent 修改入口。
- [ ] Harmony 正确显示待批准的系统估算 `72% → 91%`、活动调整原因和“成员真实反馈：未收集”；批准实际变化后才更新正式指标并递增版本，且最低成员满意度不会下降。
- [ ] Energy 正确显示 Day 2 `8.4 km → 4.6 km`、`0 → 1` 休息块、`High → Medium`；初始 8.4 km 不超过 9.0 km 硬上限。
- [ ] 暴雨重排至少有三个候选方案，默认推荐最少步行但不自动应用。
- [ ] Experience Diff 包含八个固定字段，锁定晚餐影响为“无影响”。
- [ ] 预算显示总预算、分类预算、预计花费、剩余金额和版本比较。
- [ ] 重大修改必须经用户批准后按当前正式版本递增；标准八屏全批准时呈现 Version 1 → 2 → 3 → 4 → 5，动作不得硬编码，旧版本仍可查看。
- [ ] `Reset Demo` 可以恢复初始入口；刷新、返回和直接打开子路由可恢复当前版本、草稿和历史，跳过/取消/过期/重复批准行为符合规则。
- [ ] 390px 手机和 1440px 桌面均可完成演示。
- [ ] 主要操作支持键盘，焦点清晰，状态不只靠颜色表达。
